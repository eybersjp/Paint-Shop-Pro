{
  "name": "paint-shop-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd admin && npm install && npm run build && cd .. && node scripts/build-dist.js"
  },
  "devDependencies": {}
}
